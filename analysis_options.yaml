include: package:flutter_lints/flutter.yaml

linter:
  rules:
    - prefer_relative_imports
analyzer:
  exclude:
    - "**/*.g.dart"
    - "**/*.freezed.dart"
  errors:
    invalid_annotation_target: ignore
    camel_case_types: ignore
    unused_field: ignore ignore
